import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#2b524a',
    marginBottom: 12,
    textAlign: 'center',
  },

  linkText: {
    color: '#eb4b3f',
    fontWeight: 'bold',
    textAlign: 'center',
  },
});